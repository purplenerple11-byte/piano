# Working notes for this repo

## After every push: paste the live site URL in chat

Whenever I push (branch or `main`), paste the live GitHub Pages URL in the chat
reply so it's one click away:

**https://purplenerple11-byte.github.io/piano/**

(For a specific lesson, link straight to it, e.g.
`https://purplenerple11-byte.github.io/piano/lessons/0003-single-hand-reading.html`.)

The published site serves from the `main` branch root — Pages rebuilds a minute
or two after `main` updates, so a freshly pushed change may lag briefly.
